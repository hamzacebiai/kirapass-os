/**
 * TEMPLATE — feature types (optional).
 *
 * Create this file ONLY if the feature needs shared shapes between its
 * service/controller. Delete it otherwise. Keep DTOs/return shapes here, not
 * in the controller or routes.
 */
export interface ExampleResult {
  message: string;
}
